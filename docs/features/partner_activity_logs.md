# 📋 Partner Activity Logs (파트너 활동 로그 관리)

## 📋 개요

파트너 활동 로그 관리 시스템은 파트너 시스템의 모든 활동을 추적하고 감사(Audit)하는 핵심 시스템입니다.
파트너의 모든 행동과 시스템 변경사항을 시간순으로 기록하여 투명성을 보장하고, 문제 발생 시 추적 가능한 완전한 감사 추적을 제공합니다.

> 관리자: /admin/partner/activity/logs

## 🎯 핵심 기능

### 1. 전체 활동 추적
- **파트너 신청**: 신청서 제출부터 승인까지 전 과정 추적
- **상태 변경**: 파트너 상태의 모든 변경 이력 관리
- **시스템 액션**: 자동화된 시스템 작업 기록
- **사용자 액션**: 관리자 및 파트너의 수동 작업 기록

### 2. 상세 변경 이력 관리
- **이전값 저장**: 변경 전 원본 데이터 보존
- **새로운값 기록**: 변경 후 데이터 저장
- **메타데이터**: 변경에 대한 상세 컨텍스트 정보
- **변경 사유**: 변경 이유 및 관련 메모

### 3. 보안 및 감사 추적
- **IP 주소 기록**: 모든 접근 위치 추적
- **브라우저 정보**: User-Agent를 통한 접근 환경 파악
- **사용자 식별**: 작업 수행자의 완전한 식별 정보
- **시간 기록**: 정확한 작업 수행 시점 기록

### 4. 고급 검색 및 분석
- **다중 필터링**: 활동 유형, 기간, 파트너별 필터
- **통계 분석**: 활동 패턴 및 트렌드 분석
- **데이터 내보내기**: CSV 형태의 감사 보고서 생성
- **실시간 모니터링**: 현재 진행 중인 활동 추적

## 🏗️ 데이터 구조

### 기본 로그 정보
```sql
id                  -- 고유 식별자
created_at          -- 활동 발생 시간
updated_at          -- 레코드 수정 시간
partner_id          -- 관련 파트너 ID (nullable)
application_id      -- 관련 신청서 ID (nullable)
activity_type       -- 활동 유형 (50자)
```

### 작업 수행자 정보
```sql
user_id             -- 작업 수행자 ID
user_uuid           -- 작업 수행자 UUID (샤딩 환경 지원)
ip_address          -- 접근 IP 주소
user_agent          -- 브라우저/클라이언트 정보
```

### 변경 내역 정보
```sql
old_value           -- 이전 값 (500자)
new_value           -- 새로운 값 (500자)
metadata            -- 추가 메타데이터 (JSON)
notes               -- 관리자 메모
```

## 💼 비즈니스 로직

### 1. 활동 로그 자동 기록
```php
function logActivity($activityType, $partnerId = null, $applicationId = null,
                    $oldValue = null, $newValue = null, $metadata = null, $notes = null) {

    // 현재 사용자 및 요청 정보 수집
    $userId = Auth::id();
    $userUuid = Auth::user()->uuid ?? null;
    $ipAddress = request()->ip();
    $userAgent = request()->userAgent();

    // 로그 데이터 생성
    $logData = [
        'partner_id' => $partnerId,
        'application_id' => $applicationId,
        'user_id' => $userId,
        'user_uuid' => $userUuid,
        'activity_type' => $activityType,
        'old_value' => $oldValue,
        'new_value' => $newValue,
        'metadata' => $metadata ? json_encode($metadata) : null,
        'ip_address' => $ipAddress,
        'user_agent' => $userAgent,
        'notes' => $notes,
        'created_at' => now(),
        'updated_at' => now()
    ];

    return DB::table('partner_activity_logs')->insert($logData);
}
```

### 2. 파트너별 활동 조회
```php
function getPartnerActivityHistory($partnerId, $limit = 50) {
    return DB::table('partner_activity_logs as pal')
        ->leftJoin('users as u', 'pal.user_id', '=', 'u.id')
        ->select([
            'pal.*',
            'u.name as user_name'
        ])
        ->where('pal.partner_id', $partnerId)
        ->orderBy('pal.created_at', 'desc')
        ->limit($limit)
        ->get();
}
```

### 3. 활동 통계 분석
```php
function getActivityStatistics($dateFrom, $dateTo) {
    // 활동 유형별 통계
    $activityStats = DB::table('partner_activity_logs')
        ->select('activity_type', DB::raw('count(*) as count'))
        ->whereBetween('created_at', [$dateFrom, $dateTo])
        ->groupBy('activity_type')
        ->get();

    // 일별 활동 통계
    $dailyStats = DB::table('partner_activity_logs')
        ->select(
            DB::raw('DATE(created_at) as date'),
            DB::raw('count(*) as count')
        )
        ->whereBetween('created_at', [$dateFrom, $dateTo])
        ->groupBy(DB::raw('DATE(created_at)'))
        ->orderBy('date')
        ->get();

    return [
        'activity_stats' => $activityStats,
        'daily_stats' => $dailyStats
    ];
}
```

## 📊 활동 유형별 상세

### 파트너 신청 관련
- **application_submitted**: 파트너 신청서 제출
- **status_changed**: 신청 상태 변경
- **interview_scheduled**: 면접 일정 설정
- **approved**: 파트너 승인 완료
- **rejected**: 파트너 신청 거부
- **reapplied**: 재신청 접수

### 파트너 관리 관련
- **tier_changed**: 파트너 등급 변경
- **performance_updated**: 성과 지표 업데이트
- **profile_modified**: 프로필 정보 수정
- **status_suspended**: 계정 정지
- **status_reactivated**: 계정 재활성화

### 매출 및 커미션 관련
- **sales_created**: 매출 등록
- **sales_confirmed**: 매출 확정
- **commission_calculated**: 커미션 계산
- **payment_processed**: 지급 처리

## 🎯 검색 및 필터링

### 다중 조건 검색
- **기간 검색**: 시작일/종료일 범위 지정
- **활동 유형**: 특정 활동 유형별 필터링
- **파트너별**: 특정 파트너의 활동만 조회
- **텍스트 검색**: 파트너명, 이메일, 메모 내용 검색

### 고급 필터링
- **IP 주소**: 특정 IP에서의 접근 추적
- **작업자별**: 특정 관리자의 작업 내역
- **변경사항**: 특정 값 변경이 있었던 로그만
- **메타데이터**: JSON 필드 내 특정 값 검색

## 📈 통계 및 분석

### 활동 패턴 분석
- **시간대별 활동**: 하루 중 활동이 많은 시간대
- **요일별 통계**: 요일별 활동 패턴
- **월별 트렌드**: 장기 활동 트렌드 분석
- **계절성 분석**: 특정 시기의 활동 집중도

### 사용자별 분석
- **관리자별 작업량**: 관리자별 처리 건수
- **작업 유형 분포**: 관리자별 주요 작업 유형
- **처리 속도**: 평균 처리 시간 분석
- **품질 지표**: 오류 발생률 및 재작업률

## 🔒 보안 및 데이터 보호

### 접근 제어
- **관리자 전용**: admin 미들웨어로 접근 제한
- **읽기 전용**: 로그 수정 불가, 조회만 가능
- **IP 화이트리스트**: 특정 IP에서만 접근 허용 (선택사항)
- **권한별 필터**: 관리자 권한에 따른 데이터 필터링

### 데이터 무결성
- **불변성**: 한번 생성된 로그는 수정 불가
- **완전성**: 모든 중요 작업에 대한 로그 생성 보장
- **일관성**: 표준화된 로그 형식 유지
- **가용성**: 24/7 로그 조회 가능

## 🔧 기술적 구현

### 성능 최적화
- **인덱스 최적화**: 자주 검색되는 필드에 복합 인덱스
- **파티셔닝**: 날짜별 테이블 파티셔닝으로 성능 향상
- **캐싱**: 자주 조회되는 통계 데이터 캐싱
- **배치 처리**: 대량 로그 생성 시 배치 처리

### 확장성
- **샤딩 지원**: user_uuid를 통한 분산 환경 지원
- **아카이빙**: 오래된 로그의 자동 아카이빙
- **압축**: JSON 메타데이터의 압축 저장
- **비동기 처리**: 중요하지 않은 로그의 비동기 생성

## 🔗 연관 시스템

- **Partner Users**: 파트너 기본 정보와 연동
- **Partner Applications**: 신청서 처리 과정 추적
- **Partner Sales**: 매출 관련 활동 기록
- **Partner Commissions**: 커미션 계산 과정 추적
- **Admin Users**: 관리자 작업 이력 관리

## 📋 사용 시나리오

### 1. 문제 발생 시 추적
파트너가 잘못된 등급으로 변경되었다고 신고할 경우, 해당 파트너의 tier_changed 로그를 조회하여 언제, 누가, 왜 변경했는지 추적 가능

### 2. 성과 분석
월별 승인 활동을 분석하여 승인 프로세스의 효율성 측정 및 병목 구간 파악

### 3. 보안 감사
특정 기간 동안의 모든 관리자 활동을 분석하여 보안 정책 준수 여부 확인

### 4. 시스템 모니터링
실시간으로 비정상적인 활동 패턴을 감지하여 시스템 장애나 보안 위협 조기 발견

## 🛠️ 관리자 인터페이스

### 📡 라우트 주소 (Routes)

#### 기본 라우트
- **베이스 경로**: `/admin/partner/activity/logs`
- **라우트 이름 접두사**: `admin.partner.activity.logs.`

#### 웹 라우트 목록
```php
// 로그 목록 및 관리
GET    /admin/partner/activity/logs              -> index (목록 조회)
GET    /admin/partner/activity/logs/{id}         -> show (상세 조회)
POST   /admin/partner/activity/logs              -> store (로그 생성)

// 특정 대상별 로그 조회
GET    /admin/partner/activity/logs/partner/{partnerId}      -> getPartnerLogs
GET    /admin/partner/activity/logs/application/{applicationId} -> getApplicationLogs

// 분석 및 내보내기
GET    /admin/partner/activity/logs/stats        -> stats (통계 조회)
GET    /admin/partner/activity/logs/export       -> export (CSV 내보내기)
```

#### 라라벨 라우트 이름
```php
admin.partner.activity.logs.index          // 목록 페이지
admin.partner.activity.logs.show           // 상세 페이지
admin.partner.activity.logs.store          // 로그 생성
admin.partner.activity.logs.partner        // 파트너별 로그
admin.partner.activity.logs.application    // 신청서별 로그
admin.partner.activity.logs.stats          // 통계 데이터
admin.partner.activity.logs.export         // 내보내기
```

### 로그 목록 화면 (/admin/partner/activity/logs)
- **필터링 옵션**: 활동 유형, 날짜 범위, 파트너별 검색
- **통계 카드**: 총 로그 수, 오늘 활동, 승인 활동, 상태 변경
- **테이블 뷰**: ID, 시간, 파트너, 활동 유형, 변경값, 작업자, IP
- **액션 버튼**: 내보내기, 통계 보기, 상세 조회

### 로그 상세 화면 (/admin/partner/activity/logs/{id})
- **기본 정보**: 로그 ID, 활동 시간, 유형, 작업자
- **파트너 정보**: 파트너명, 이메일, 코드, 신청서 정보
- **변경 내역**: 이전값 vs 새로운값 비교 표시
- **메타데이터**: JSON 형태의 상세 정보
- **접속 정보**: IP 주소, User-Agent
- **관련 로그**: 동일 파트너/신청서의 다른 로그

### API 엔드포인트
- **GET** `/admin/partner/activity/logs` - 로그 목록
- **GET** `/admin/partner/activity/logs/{id}` - 로그 상세
- **GET** `/admin/partner/activity/logs/partner/{partnerId}` - 파트너별 로그
- **GET** `/admin/partner/activity/logs/application/{applicationId}` - 신청서별 로그
- **GET** `/admin/partner/activity/logs/stats` - 통계 데이터
- **GET** `/admin/partner/activity/logs/export` - CSV 내보내기
- **POST** `/admin/partner/activity/logs` - 로그 생성 (API)

---
*파트너 시스템의 투명성과 신뢰성을 보장하는 핵심 감사 시스템*
